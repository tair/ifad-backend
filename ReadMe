![]

# IFAD Backend

This is the backend server that handles the ingestion of relevant files and handles the querying of annotations and gene info from the files.


## Backend Setup

``` bash
# install dependencies
yarn

# run main server
yarn start

# run automated tests
yarn test

# kill server
ctrl+c
```
